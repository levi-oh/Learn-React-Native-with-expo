import React from "react";
import styled from "styled-components";

const Course = (props) => (
  <Container>
    <Cover>
      <Logo sources={} />
      <Title></Title>
      <SubTitle></SubTitle>
    </Cover>
  </Container>
);

export default Course;
